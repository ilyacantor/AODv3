# Deferred work — aod

1. 2026-04-09 | https://claude.ai/chat/7aaa8044-1727-472d-9aaa-8e830c4da9e9 | src/aod/db/pool.py get_pool() | retry-forever loop when create_pool() raises. Pool never recovers — turns transient hiccup into permanent 500s until process restart. Severity: blocker. Blocking: AOD availability degrades silently after any pool init failure.

2. 2026-04-09 | https://claude.ai/chat/4457c1b3-e992-46ca-ac6e-1c6f737b0bbf | static/js/app.js:3342 | populateTenantsFromFarm dropdown displays UUID instead of entity_id. Same I2/I5 violation class as runs list/detail panel fix that landed. Severity: degraded. Blocking: UUID-in-UI consistency. RESOLVED 2026-06-10 (commit 59faea9) — /api/farm/tenants now returns entity_labels (tenant_id → entity_id from Farm snapshot rows; pre-identity-split rows label==tenant by construction) and populateTenantsFromFarm labels options with the entity while option values stay machine tenant UUIDs. Verified live: 100/100 tenants labeled (e.g. e95c1fa2→ApexWave-b0a4); full_pipeline_journey.spec.ts drives the dropdown by entity name.

3. 2026-05-16 | ws-1 b5 cleanup | aod repo-wide (6 remaining files) | After Block 5 scrub of demo-critical src/aod/models/output_contracts.py (Pydantic Field example), 6 files retain banned literals in categories (c/d): tests/mai/test_status_endpoint.py + tests/test_pipeline_identity.py (test fixtures using meridian/cascadia tenant tags — exempted by hook tests/* rule), CLAUDE.md + AOS_MASTER_RACI_v8_6.csv + convergence_{blueprint,transition}_master.md (policy/spec). | severity: cosmetic | blocking: nothing

4. 2026-06-03 | from-farm-silent-sync | POST /api/runs/from-farm sync_status + AOD→AAM handoff | A1 SILENT-SUCCESS (found during AAM full-pipeline verification, see aam_deferred_work.md #51). POST /api/runs/from-farm returns sync_status="synced" but does NOT actually deliver candidates to AAM. Repro: from-farm on Farm snapshot 6b87267a → run_f1916b429abf (444 observations → 101 candidates; response sync_status="synced", sync_error=null) — yet AAM /api/aam/candidates stayed 0 and AAM /api/handoff/aod/debug/last-receive STILL showed the prior r8-iso handoff (nothing arrived). Only the EXPLICIT POST /api/handoff/aam/export?aod_discovery_id=run_f1916b429abf actually delivered (92 candidates → AAM /receive → 93 pipes). So from-farm's auto-sync claims success on a push that never reached AAM — a fake success (A1: "HTTP 200 when the underlying operation failed" / silent fallback; an operator trusting sync_status="synced" believes AAM has the data when it does not). Fix: from-farm must either (a) actually POST the candidates to AAM /api/handoff/aod/receive (or call the same path /handoff/aam/export uses) and propagate the REAL AAM result into sync_status/sync_error, or (b) report sync_status="not_attempted" / the true failure — never "synced" for a delivery that did not happen. | severity: degraded | blocking: trustworthy AOD→AAM auto-handoff (today operators must call /api/handoff/aam/export explicitly; the "synced" signal is misleading)
