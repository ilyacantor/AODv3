Good: the split-brain “HAS_DISCOVERY” inconsistency is likely gone.

Bad: **this run regressed hard on key normalization and admission alignment**. The report shows **KEY_NORMALIZATION_MISMATCH = 14** and **zombies 12 missed** again. 

### What the report says, plainly

* **Shadows:** expected 72, AOD found 73, but only **68 matched** → **4 missed, 4 FP**. 
  Missed shadows include `amazon.com` and `workers.dev` with RCA = `KEY_NORMALIZATION_MISMATCH`. 
* **Zombies:** expected 37, AOD found 25, matched 25 → **12 missed** (all the named misses are `KEY_NORMALIZATION_MISMATCH`). 
* **Admission:** Farm expected admit for 15 assets that AOD rejected, including `amazon.com`, `tiktok.com`, `typekit.net`, `workers.dev`, `datadoghq.com`. 
  And AOD admitted some Farm-rejected single-source assets (`openforce.net`, `salesforce-crm.com`, `netbox.net`). 

### Real root cause now

**Your “hard freeze” touched discovery semantics, but canonical key selection/merge behavior is still not deterministically producing Farm’s expected key.** The report’s RCA is screaming that. 

Also: your earlier P0 diagnostic table that showed `rapidspace.co` etc rejected correctly is not reflected here—this run shows different offenders. So either:

* different code path is used in the assessment run, or
* admission engine is correct but the *pipeline feeding it* is producing different canonical keys / discovery counts.

---

## What to do next (short, CTO)

### 1) Prove where key drift re-entered (one test)

For each of these keys (pick 5):
`amazon.com`, `workers.dev`, `probase.com`, `fasthub.dev`, `syncdesk.tech`

Print a single line each:

* `observed_domains` (top 5)
* `candidate_registrable_domains` (set)
* `chosen_canonical_key`
* `why_chosen` (score components)
* `final_emitted_asset_key`

**Goal:** determine if drift is in:

* `choose_canonical_domain()` (bad scoring / wrong candidates), OR
* a **later merge/alias step** overriding the chosen key, OR
* reconciliation reading a different key field than you think.

Because the report explicitly says “domain exists in AOD evidence but not used as canonical key.” 

### 2) Add a “no-override” invariant (kill the late-binding merge bug)

Once `canonical_key` is chosen, it must never change.

Add invariant:

* `asset.asset_key == canonical_key_selected`
* if violated → fail run

This is the *same philosophy as split-brain*: make it impossible for a later stage to undo identity.

### 3) Fix admission mismatch second

Your admission FPs are all single-source assets (`openforce.net`, `salesforce-crm.com`, `netbox.net`). 
So enforce a hard guardrail in admission pipeline (not just engine):

* if `discovery_sources_count < 2` and not governed (idp/cmdb/cloud) → reject
* regardless of finance

(You already think this is true; this run shows it isn’t being applied consistently.)

---

## Bottom line

The “hard freeze” didn’t solve the real bottleneck: **canonical key stability across the full pipeline**. The assessment proves key drift is back and it’s driving both zombie misses and shadow misses. 

If you want one decisive move: **add the canonical-key no-override invariant** and run again. That will immediately tell you whether a late merge step is undoing your canonical selection.
