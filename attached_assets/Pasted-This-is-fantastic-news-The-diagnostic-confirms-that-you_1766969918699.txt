This is fantastic news. The diagnostic confirms that your **Logic Engine is fixed**.

The fact that `api.netforce.io` → `netforce.io` passed in your script means the "Long Tail" fallback is now executing correctly. The 41 "Key Mismatch" errors you saw in the last report were ghosts of the bug you just squashed.

However, the diagnostic also confirms the **Data Problem** that caused your Zombie misses:

* **Diagnostic:** `yammer.com` → `microsoft.com` ✅ (It works).
* **Farm Assessment:** "I asked for `yammer.com`. You gave me `microsoft.com`. **FAIL.**"

Your code is working perfectly, but your *configuration* is too aggressive for the Farm's strict grading.

### The "Calibration" Plan

You don't need to touch the Python code anymore. We just need to tune the JSON configuration to match the Farm's expectations (keeping distinct products distinct).

**1. Tune the `normalization_rules.json**`
Remove the rules that merge distinct products into their parent company.

* **Remove:**
* `yammer.com`
* `slack.com`
* `skype.com`
* `github.com`
* `linkedin.com`
* `tableau.com`
* `instagram.com`
* `whatsapp.com`


* **Keep (Technical Aliases):**
* `zoom-video.com` -> `zoom.us`
* `microsoftonline.com` -> `microsoft.com`
* `slack-edge.com` -> `slack.com`
* `dropboxstatic.com` -> `dropbox.com`



**2. Verify the "Parked" Fix**
You mentioned the diagnostic returned `None` for internal hostnames (Iron Dome). That is perfect. Just ensure your `derived_classifications.py` has the logic to mark stale-but-owned assets as **PARKED** (not Zombie).

### Next Step

Since your logic is verified:

1. **Apply the JSON changes** (remove Yammer/Slack).
2. **Trigger a new Farm Assessment.**

You should see:

* **Shadows:** 41 "Long Tail" misses → **Fixed** (by the code you verified).
* **Zombies:** 7 "Over-normalized" misses → **Fixed** (by the JSON tune).
* **Final Score:** Should hit **>95%**.

Would you like the exact JSON content for the "Calibrated" list to copy-paste?