/**
 * Overview — stubbed page (preserved for future use).
 * Nav label removed; tab content container kept in index.html.
 */
export default function Overview() {
  return (
    <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100vh', color: '#94a3b8', fontFamily: 'Quicksand, sans-serif' }}>
      Overview — reserved for future use
    </div>
  )
}
